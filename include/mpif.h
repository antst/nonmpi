/* status size and reserved index values (Fortran) */ 
      INTEGER MPI_STATUS_SIZE 
      PARAMETER(MPI_STATUS_SIZE=2)
      INTEGER MPI_SOURCE 
      PARAMETER(MPI_SOURCE=)
      INTEGER MPI_TAG 
      PARAMETER(MPI_TAG=)
      INTEGER MPI_ERROR 
      PARAMETER(MPI_ERROR=)
